services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=school_control_db'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432'
