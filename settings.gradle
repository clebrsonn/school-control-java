plugins {
    id "org.gradle.toolchains.foojay-resolver" version "0.8.0"
    // Try applying the convention plugin as well, though often the resolver alone is enough.
    id "org.gradle.toolchains.foojay-toolchains-repositories-convention" version "0.8.0"
}

rootProject.name = 'school-control'
